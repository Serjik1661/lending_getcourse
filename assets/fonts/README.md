# Fonts

В проекте используются локальные WOFF2-файлы:

- `Geologica` — заголовки и акцентная типографика.
- `Mulish` — основной текст.

Шрифты подключены локально в `assets/css/variables.css`, внешние CDN не используются.
